const selectFavoriteOffers = (state) => state.FAVORITES.favoriteOffers;

const selectIsFavoriteOffersLoaded = (state) => state.FAVORITES.isFavoriteOffersLoaded;

export {selectFavoriteOffers, selectIsFavoriteOffersLoaded};
